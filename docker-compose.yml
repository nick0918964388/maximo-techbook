services:
  mkdocs-site:
    image: nginx:stable-alpine
    container_name: mkdocs_web_server
    ports:
      - "8000:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
    restart: unless-stopped
